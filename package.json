{
  "name": "redux-saga-offline",
  "version": "1.0.0-dev",
  "description": "Sagas and reducers for offline-first support",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir dist --ignore '**/__tests__/**'"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.4",
    "redux-persist": "^5.3.3"
  },
  "peerDependencies": {
    "redux": ">=3.0.0",
    "redux-saga": ">=0.16.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-latest": "^6.24.1",
    "babel-preset-stage-3": "^6.24.1",
    "jest": "^21.2.1"
  }
}
